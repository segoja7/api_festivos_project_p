apiVersion: v1
kind: Service
metadata:
  name: festivos-frontend-service
  namespace: festivos-api
  labels:
    app: festivos-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: festivos-frontend
